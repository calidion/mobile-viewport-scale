require.config({
  baseUrl: '',
  paths: {
    device: 'data/device',
    os: 'data/os',
    browser: 'data/browser',

    viewport: 'data/viewport',

    map: 'map',
    mobileViewportScale: 'index',
    mvs: 'mvs'
  }
});