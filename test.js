require.config({
  baseUrl: '',
  paths: {
    os: 'data/os',
    browser: 'data/browser',

    map: 'map',
    mobileViewportScale: 'mobile-viewport-scale',
    boot: 'boot'
  },
  deps: ['boot']
});
